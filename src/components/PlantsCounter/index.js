import Proptypes from "prop-types";
import "./style.scss";

const PlantsCounter = () => (
  <div className="section-plant-counter">
    <div className="plant-counter">
      <p>Grâce à vous, déjà 684 plantes recensées</p>
    </div>
  </div>
);

PlantsCounter.propTypes = {};

export default PlantsCounter;
